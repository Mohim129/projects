const product = {
    name: 'Laptop',
    price: 999,
    description: "i5 processor, 8GB RAM, 256GB SSD"
}

// const newPrice = product.price + 100;
// const productName = `this is a ${product.name}`;
// const name = product.name


// console.log(newPrice , name );
const {name, price} = {name: 'iPhone',price: 799, description: "A14 Bionic chip, 6GB RAM, 128GB storage"};
console.log(name, price);